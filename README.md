# aco_simulation
Thes "main.py" python script simulates ACO in a world, represented as a matrix with walkable squares (0) and blocked squares (1).
The more kompelx version "simulation2.py" takes thsi approach and uses it to fidn the shrottest path in a graph made of n Nodes.
